% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_mixed_effect.R
\name{plot_mixed_effect}
\alias{plot_mixed_effect}
\title{Plot mixed-model predictions with raw data and species means}
\usage{
plot_mixed_effect(
  model,
  data,
  xvar,
  yvar = "midpoint_stride_speed",
  ylab = "Midpoint stride speed",
  xlab = "",
  title = "",
  caption = "",
  sizevar = "mass_mg",
  group_var = "ant_group",
  id_var = "Individuum",
  species_var = "Species",
  point_alpha = 0.22,
  point_size = 1.5,
  mean_size = 2.8,
  line_size = 1.1,
  label_size = 3.2,
  size_label = "Body size in mm",
  show_ci = TRUE,
  show_species_labels = TRUE,
  add_lit_line = FALSE
)
}
\arguments{
\item{model}{Fitted mixed-effects model object (compatible with
\code{ggeffects::ggpredict()}).}

\item{data}{Data frame used to fit \code{model}.}

\item{xvar}{Character scalar; name of the focal predictor on the x-axis.}

\item{yvar}{Character scalar; name of the response variable
(default: \code{"midpoint_stride_speed"}).}

\item{ylab}{Character scalar; y-axis label.}

\item{xlab}{Character scalar; x-axis label.}

\item{title}{Character scalar; plot title.}

\item{caption}{Character scalar; plot caption. If empty, a default caption
describing all plot elements is used.}

\item{sizevar}{Character scalar; name of variable mapped to point size
(default: \code{"mass_mg"}).}

\item{group_var}{Character scalar; grouping variable (default: \code{"ant_group"}).}

\item{id_var}{Character scalar; individual identifier (default: \code{"Individuum"}).}

\item{species_var}{Character scalar; species identifier (default: \code{"Species"}).}

\item{point_alpha}{Numeric; alpha for raw points.}

\item{point_size}{Numeric; base size for raw points (unused if size mapped).}

\item{mean_size}{Numeric; base size for mean points (unused if size mapped).}

\item{line_size}{Numeric; line width for prediction lines.}

\item{label_size}{Numeric; text size for species labels.}

\item{show_ci}{Logical; if \code{TRUE}, draw prediction ribbons.}

\item{show_species_labels}{Logical; if \code{TRUE}, label species means.}

\item{add_lit_line}{Logical; if \code{TRUE}, add a literature reference line.}
}
\value{
A \code{ggplot} object.
}
\description{
This function plots a continuous predictor against midpoint stride speed,
overlaying raw observations, individual/species means, unadjusted group-wise
linear fits, and conditional mixed-model predictions by ant group.
}
\details{
Raw points show all observations. Filled points show individual means
(grouped by \code{id_var} and \code{species_var}). Dashed lines are ordinary
least-squares fits to the raw data within each ant group, summarizing the
unadjusted association between \code{xvar} and \code{yvar}. Solid coloured
lines and ribbons are population-level mixed-model predictions from
\code{ggeffects::ggpredict()}, representing the conditional effect of
\code{xvar} on \code{yvar} by ant group, adjusted for body length and other
kinematic covariates and averaging over random effects. Because these
conditional prediction curves are adjusted, they do not necessarily pass
through the raw point cloud or the individual means.
}
\examples{
# plot_mixed_effect(m_speed, d_speed, xvar = "scale_swing_time",
#   xlab = "Swing time (scaled)",
#   ylab = expression("Midpoint stride speed ("*mm~s^{-1}*")"),
#   title = "Conditional effect of swing time on midpoint stride speed")
}

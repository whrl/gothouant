% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rot_shift.R
\name{rot_shift}
\alias{rot_shift}
\title{Rotate and shift trajectories into another frame}
\usage{
rot_shift(phi, rp, p, side, norm_factor)
}
\arguments{
\item{phi}{Numeric, rotation angle in radians (0 = original x-axis).
Typically derived from the body axis or net direction of progression.}

\item{rp}{Data frame or list with numeric components \code{x} and
\code{y} used as reference point (e.g. body center).}

\item{p}{Data frame with numeric columns \code{x} and \code{y} to
transform.}

\item{side}{Character, \code{"L"} or \code{"R"}; right side is mirrored.}

\item{norm_factor}{Numeric, normalizing factor for \code{u} and
\code{v} (e.g. body length).}
}
\value{
A copy of \code{p} with added columns:
\describe{
\item{u}{Lateral coordinate (left-positive) in normalized units.}
\item{v}{Fore-aft coordinate in normalized units.}
}
}
\description{
Translates a trajectory so that a reference point lies at the origin,
rotates it by \code{phi}, and optionally mirrors the right side so
that left and right legs become comparable. Coordinates are normalized
(e.g. by body length) so that \code{u} and \code{v} can be interpreted
in relative units.
}
